streamlit==1.35.0
langchain==0.2.14
langchain-core==0.2.39
langchain-community==0.2.12
langchain_groq==0.1.9
pandas==2.0.2
python-dotenv  # To load environment variables (e.g., API keys from a .env file)
openai         # If you're using OpenAI's API for generating text
requests       # For making HTTP requests (e.g., API calls)
validators     # For validating URLs if you're dealing with links
tiktoken       # To handle tokenization for OpenAI API
